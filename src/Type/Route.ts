// Handle URL as File Router within Route Directory, PHP Style
    // Default, can be changed with config in Server or dropped completely for an API
    
export interface Route {
    asHTML: string | Promise<string>
}